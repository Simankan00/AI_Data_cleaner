version: '3.8'

services:
  cleaning-agent:
    build: .
    container_name: data_cleaning_agent
    ports:
      - "8501:8501"
    volumes:
      - ./.cleaning_rag_store:/app/.cleaning_rag_store
    environment:
      - TQDM_DISABLE=1
    restart: unless-stopped